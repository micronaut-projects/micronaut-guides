name: Test micronaut-mqtt
on:
  push:
    branches:
      - master
      - '[1-9]+.[0-9]+.x'
    paths: ["version.txt","guides/micronaut-mqtt/**"]
  pull_request:
    branches:
      - master
      - '[1-9]+.[0-9]+.x'
    paths: ["version.txt","guides/micronaut-mqtt/**"]
jobs:
  maven-test:
    if: ${{ true }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: ["8","11","17"]
        testproject: ["micronaut-mqtt-maven-java/app","micronaut-mqtt-maven-java/cli","micronaut-mqtt-maven-groovy/app","micronaut-mqtt-maven-groovy/cli","micronaut-mqtt-maven-kotlin/app","micronaut-mqtt-maven-kotlin/cli"]
    env:
      JDK_VERSION:  ${{ matrix.java }}
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: actions/cache@v2
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
      - name: Build Projects
        run: ./gradlew micronautMqttGenerateProjects
      - name: Build with Maven
        run: 'cd build/code/micronaut-mqtt/${{ matrix.testproject }};./mvnw test'
  gradle-test:
    if: ${{ true }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: ["8","11","17"]
        testproject: ["micronaut-mqtt-gradle-java/app","micronaut-mqtt-gradle-java/cli","micronaut-mqtt-gradle-groovy/app","micronaut-mqtt-gradle-groovy/cli","micronaut-mqtt-gradle-kotlin/app","micronaut-mqtt-gradle-kotlin/cli"]
    env:
      JDK_VERSION: ${{ matrix.java }}
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: actions/cache@v2
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
      - name: Build Projects
        run: ./gradlew micronautMqttGenerateProjects
      - name: Build with Gradle
        run: 'cd build/code/micronaut-mqtt/${{ matrix.testproject }};./gradlew test'
