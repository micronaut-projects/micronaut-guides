interface:
  display_name: "Micronaut Guides Infrastructure"
  short_description: "Change guide generation and build internals"
  default_prompt: "Use $micronaut-guides-infrastructure to modify the Micronaut Guides generator, macros, metadata schema, or Gradle task wiring."
policy:
  allow_implicit_invocation: true
