interface:
  display_name: "Micronaut Guide Validation"
  short_description: "Build and test generated guide projects"
  default_prompt: "Use $micronaut-guides-validation to build, render, and test the affected Micronaut guide."
policy:
  allow_implicit_invocation: true
