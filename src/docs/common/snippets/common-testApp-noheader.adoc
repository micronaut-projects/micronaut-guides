To run the tests:

:exclude-for-build:maven,pyronaut

[source, bash]
----
./gradlew test
----

Then open `build/reports/tests/test/index.html` in a browser to see the results.

:exclude-for-build:

:exclude-for-build:gradle,pyronaut

[source, bash]
----
./mvnw test
----

:exclude-for-build:

:exclude-for-build:gradle,maven

[source, bash]
----
pyronaut install
pyronaut validate-config
pyronaut test
----

:exclude-for-build:
