pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "micronaut-guides-buildsrc"

apply from: file('../gradle/local-composite-builds.gradle')
