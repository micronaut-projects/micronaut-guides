micronaut:
  application:
    name: api
  config-client:
    enabled: false # <1>
  http:
    services:
      users:
        urls:
          - http://localhost:8081 # <2>
      orders:
        urls:
          - http://localhost:8082 # <3>
kubernetes:
  client:
    secrets:
      enabled: false # <4>