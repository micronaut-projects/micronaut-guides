micronaut:
  application:
    name: api
  config-client:
    enabled: false # <1>
  http:
    services:
      users:
        urls:
          - http://localhost:8081
      orders:
        urls:
          - http://localhost:8082
kubernetes:
  client:
    secrets:
      enabled: false # <2>