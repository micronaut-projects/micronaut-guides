Run the next command to delete `micronaut-k8s/{0}` artifacts container repository:

[source,bash,subs="attributes+"]
----
oci artifacts container repository delete --repository-id ${1}_REPOSITORY --force
----

