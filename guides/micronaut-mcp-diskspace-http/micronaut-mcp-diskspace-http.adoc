common:header.adoc[]

common:requirements.adoc[]

common:completesolution.adoc[]

common:cli-or-launch.adoc[]

== Dependency

Add the https://micronaut-projects.github.io/micronaut-mcp/latest/guide#serverJavaSdk[Micronaut MCP Server Java SDK] dependency to your build file:

dependency:micronaut-mcp-server-java-sdk[groupId=io.micronaut.mcp,scope=compile]

== Configuration

resource:application.properties[tag=mcp]

<.> https://modelcontextprotocol.io/specification/2025-11-25/basic/transports#streamable-http[To use Streamable HTTP]

== Disk Utils

Create a utility class to get the disk space information of the computer.

source:DiskUtils[]

== MCP Tool

Next, we are going to create an MCP Tool that exposes the disk space information.

____
Tools: Executable functions that allow models to perform actions or retrieve information
____

source:MyTools[]

callout:singleton[]
<.> Annotate a method with `@Tool` to expose it as an https://micronaut-projects.github.io/micronaut-mcp/snapshot/guide/#tools[MCP Tool].


common:runApp.adoc[]

=== MCP Inspector

To test the MCP Server, run the https://modelcontextprotocol.io/docs/tools/inspector[MCP inspector].

Select `Streamable HTTP` as `Transport type`.

Enter `http://localhost:8080/mcp` as the URL.

Click connect. You will be able to list tools and invoke the `freeDiskSpace` tool.

image::mcp/mcp-diskspace-http.png[]

== Tests

=== Test Dependencies

Add the https://micronaut-projects.github.io/micronaut-mcp/latest/guide/#clientJavaSdk[Micronaut Java SDK MCP Client] dependency to your build file:

dependency:micronaut-mcp-client-java-sdk[groupId=io.micronaut.mcp,scope=test]


=== Test

The following test verifies the application exposes an invokable tool.

test:MyToolsTest[]

common:testApp.adoc[]