=== Test

Create a test to verify that when you make a GET request to `/hello` you get `Hello World` as a response:

:exclude-for-languages:python

test:HelloControllerTest[]

callout:micronaut-test[]
callout:http-client[]
callout:http-request[]

:exclude-for-languages:

:exclude-for-languages:java,groovy,kotlin

test:HelloControllerTest[]

<1> Start the application for the pytest fixture.
<2> Send an HTTP request with the `requests` library.
<3> Verify the response body.

:exclude-for-languages:
