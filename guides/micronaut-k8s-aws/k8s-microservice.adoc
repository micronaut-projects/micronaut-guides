Edit `k8s.yml` inside `{0}` service.

[source, yaml]
./{0}/k8s.yml
----
include::{sourceDir}/micronaut-k8s-aws/@sourceDir@/{0}/k8s.yml[]
----
<1> Repository URI that we created in the ECR container registry. Change `<aws-region>` to your region and change `<aws-account-id>` to your AWS account ID. You can achieve this by running `sed -i'' -e "s/<aws-region>/$AWS_REGION/" {0}/k8s.yml` and `sed -i'' -e "s/<aws-account-id>/$AWS_ACCOUNT_ID/" {0}/k8s.yml`.
<2> Change imagePullPolicy to Always.
