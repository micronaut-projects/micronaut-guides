type Query {
    bookById(id: ID): Book # <1>
}

type Book { # <2>
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author { # <3>
    id: ID
    firstName: String
    lastName: String
}
